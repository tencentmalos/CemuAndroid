# G4 preCalc probe — macOS runtime validation

Date: 2026-09-13. Host: macOS (Apple Silicon), Cemu desktop RelWithDebInfo
(`build_no_vcpkg`, `bin/Cemu_relwithdebinfo`, emulator hash cb0839ab), Vulkan
(MoltenVK) backend. Title: BotW JP v208 (`00050000101C9300`), launched from the
v208+DLC `.wua`.

## Package
- recipe: `games/reverse/botw/wiiu-v208/patches/precalc-probe/v1/build.json`
- module digest: `632c3d145bc22e40b390cbabfb6d8792827117a6be34c7c9b374fba4f01dbdd1`
- image: 500 bytes; sections text/rodata/data/bss; 7 symbols; 23 relocations; 1 hook
- source RPX SHA-256 bound: `ba58da5b95ce929e005d058ceb08b9b2788d1ab2bbc8a6c189bbadca0bb34d30`
- toolchain: Homebrew clang 23.1.1 + lld (PPC32 BE), doctor receipt verified
- verify.py: PASS (digest/image/sections/relocations/symbols/hooks/identity)

## Hook (evidence-backed entry hook + original trampoline)
- true entry `System::preCalc` = `0x02C57E4C` (`mflr r0`, `7c0802a6`), verified
  from decompressed RPX text and live runtime; the BetterVR profiler hooks
  `+0x4`, which is mid-prologue and would clobber the live r0/args — this probe
  hooks the real entry instead.
- installed branch at `0x02C57E4C`: `4aba832c` = `b 0x01800178` (non-linking,
  AA=0), decoded target matches the ASM bridge exactly.
- original trampoline `0x018001F4`: replays `mflr r0` + resume `b 0x02C57E50`.
- ASM bridge saves/restores r3..r10 + LR around a no-arg observer, then
  tail-calls the trampoline, preserving BotW's argument registers (fixes an
  initial crash where the C wrapper clobbered r4).

## Runtime results (read live via Guest GDB remote)
- `guest_patch_status`: module `u-king` Active, gen 1, base `0x01800000`, sdk=yes;
  SDK gateway `0x00e04ee4`, contexts=1, calls grew 7→24, rejects=0.
- `g_precalc_hit_count` = 2530 — entry hook fires once per logic frame; game
  runs continuously (29 FPS, 391 draws, real gameplay load screen) with no crash.
- `g_sdk_last_status` = 0 (Ok) — math.transform_batch dispatched through the
  real r3..r8 HLE gateway.
- `g_sdk_ok_count` = 20 — host transform produced exactly (11,22,33) (row-major
  4x4 * point), matched against the guest's expected value.
- `g_sdk_last_x` raw `41300000` = float 11.0 — big-endian wire read-back correct.

## Evidence level
- generation/install/execution/ABI/SDK/function: PASS (real gameplay).
- retire-on-title-stop: logic wired to NotifyModuleUnloaded; not force-triggered
  headlessly this run (no debugbus stop-title command). Clean re-boot after a
  prior crash confirms no persistent corruption.
- render scope: producer/consumer path built and counted (0 this run — the probe
  does not emit scopes; exercised separately in G3 unit path).
